[project]
name = "quality-checker-workspace"
version = "0.1.0"
description = "Desktop quality checker for SOM and eDCT workbooks"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openpyxl>=3.1.5",
    "pandas>=3.0.2",
    "PyQt6>=6.7.1",
]
